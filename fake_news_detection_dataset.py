# -*- coding: utf-8 -*-
"""FAKE NEWS DETECTION DATASET.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1_0ELiy0K1nh-A9kO_t1I0LRKQvij8u7h
"""

import pandas as pd
import numpy as np
import string
import re

from sklearn.model_selection import train_test_split
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import classification_report, accuracy_score

# Initialize an empty DataFrame
df = pd.DataFrame()

# Load the dataset
file_path = '/content/Fake News Detection Dataset.csv'  # Replace with the correct path if needed
try:
    df = pd.read_csv(file_path)  # Load if file exists
except FileNotFoundError:
    print(f"Error: File not found at {file_path}. Please check the file path and ensure the file exists.")
    # Handle file not found, e.g., ask for a new path or exit
    # For this example, we'll just continue with the empty DataFrame
    # but in a real scenario, you'd want to address the missing file

# Now you can safely use df, even if the file wasn't found
# Display basic info
print("Dataset shape:", df.shape)
print("Columns:", df.columns)
print("First few rows:\n", df.head())

# ... (rest of the code remains the same)